import { createStitches } from '@stitches/react';

export const { styled, css, theme, createTheme } = createStitches({
  theme: {
    colors: {
      blackOne: '#151517',
      blackTwo: '#242425',
      blackThree: '#2E2E2E',
      text: '#FFFFFF',
      primary: '#FF2729',
    },
  },
});
